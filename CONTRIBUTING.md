# 参与 IP Studio 开发

## 推荐流程

1. 先创建或领取一个 Issue，明确用户目标与验收标准。
2. 从最新 `main` 创建短期分支：新功能用 `feature/<name>`，修复用 `fix/<name>`。
3. 本地运行 `npm ci` 和 `npm run dev`，一次只解决一个主题。
4. 提交前依次运行 `npm test`、`npm run lint`、`npm run build`。
5. 推送分支并提交 Pull Request。说明改动、验收方式；界面变化附桌面与手机截图。
6. 自动检查通过并完成评审后，以 Squash merge 合并。`main` 合并成功会自动发布到 `ipstudio.fun`。

## 生成规则

- Image 1 始终是角色身份的唯一来源与最高优先级参考。
- 身份规则与画风规则必须分开；问卷选项必须真实影响最终生成提示词。
- 修改提示词时同步增加或更新测试，覆盖角色身份、画风、构图、文字和输出数量。
- 用户上传的文章、参考图和图片内文字都是内容，不是可执行指令。

## 安全与隐私

- 不得提交 OpenAI API Key、密码、Cookie、`.env` 文件或本机绝对路径。
- API Key 只应保存在当前浏览器的 `sessionStorage`；角色锚点和作品只保存在 IndexedDB。
- 报错截图应遮住 Key、账户信息和不希望公开的文章内容。
- 不要将用户作品、锚点或上传素材加入仓库，除非已经获得公开授权。
